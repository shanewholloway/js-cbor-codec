{
  "private": true,
  "dependencies": {
    "abstract-leveldown": "^5.0.0",
    "cbor-codec": "file:../..",
    "encoding-down": "^5.0.0",
    "levelup": "^4.4.0",
    "memdown": "^5.1.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "sinon": "^9.2.2",
    "tape": "^5.0.1"
  },
  "scripts": {
    "pretest": "test -d ./node_modules || npm -s install",
    "test": "node test-leveldown.js"
  }
}
